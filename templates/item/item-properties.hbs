<section class="tab {{tab.cssClass}}" data-tab="properties" data-group="primary">
    <div class="form-group">
        <label>Grid Width</label>
        <input type="number" name="system.width" value="{{system.width}}">
    </div>
    <div class="form-group">
        <label>Grid Height</label>
        <input type="number" name="system.height" value="{{system.height}}">
    </div>
</section>